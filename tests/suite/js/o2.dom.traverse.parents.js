(function(window, o2) {
    'use strict';

    var u      = o2.Unit,
        assert = u.assert,
        run    = u.run,
        add    = u.add,

        Suite = {
            tests : 'dom.traverse.parents',
            run : function() {

            }
        };
}(this, this.o2));

